<Window x:Class="MathSolverApplication.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MathSolverApplication"
        mc:Ignorable="d"
        Title="Math Equation Solver" Height="500" Width="550"
        Background="#F5F5F5" WindowStartupLocation="CenterScreen" >
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Math Equation Solver" HorizontalAlignment="Center" Foreground="#2C3E50" FontWeight="Bold" FontSize="20" Margin="0,0,0,20"/>


        <TabControl Grid.Row="1" Name="tabControl">
            <TabItem Header="Quadratic Formula" FontSize="13">
                <Grid Margin="20">
                    <StackPanel>
                        <TextBlock Text="Solve ax² + bx + c" FontSize="13" FontWeight="SemiBold" Foreground="#34495E" Margin="0,0,0,20"/>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
                            <TextBlock Text="a = " Width="40" VerticalAlignment="Center" FontSize="14" FontWeight="SemiBold"/>
                            <TextBox x:Name="txtA" Width="200" Height="32" FontSize="14" Padding="8,5"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
                            <TextBlock Text="b = " Width="40" VerticalAlignment="Center" FontSize="14" FontWeight="SemiBold"/>
                            <TextBox x:Name="txtB" Width="200" Height="32" FontSize="14" Padding="8,5"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
                            <TextBlock Text="c = " Width="40" VerticalAlignment="Center" FontSize="14" FontWeight="SemiBold"/>
                            <TextBox x:Name="txtC" Width="200" Height="32" FontSize="14" Padding="8,5"/>
                        </StackPanel>
                        <Button x:Name="btnCalculateQuad" Content="Calculate" Width="200" Height="38" Background="#3498DB" Foreground="White" FontSize="15" FontWeight="Bold" Click="CalculateQuadratic" BorderThickness="0" Margin="0,0,0,20" Cursor="Hand">
                            <Button.Style>
                                <Style TargetType="Button">
                                    <Setter Property="Background" Value="#3498DB"/>
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="Button">
                                                <Border Background="{TemplateBinding Background}" CornerRadius="20" BorderThickness="0">
                                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                </Border>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </Button.Style>
                        </Button>
                        <Border Background="#ECF0F1" Padding="15" CornerRadius="5" BorderBrush="#BDC3C7" BorderThickness="1">
                            <TextBlock x:Name="txtQuadResult" Text="Enter Values and click Calculate" FontSize="13" Foreground="#2C3E50"/>
                        </Border>
                    </StackPanel>
                </Grid>
            </TabItem>
            <TabItem Header="Area of a Circle" FontSize="13">
                <Grid Margin="20">
                    <StackPanel>
                        <TextBlock Text="Solve A = π × r²" FontSize="13" FontWeight="SemiBold" Foreground="#34495E" Margin="0,0,0,20"/>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
                            <TextBlock Text="r = " Width="40" VerticalAlignment="Center" FontSize="14" FontWeight="SemiBold"/>
                            <TextBox x:Name="txtR" Width="200" Height="32" FontSize="14" Padding="8,5"/>
                        </StackPanel>
                        <Button x:Name="btnCalculateArea" Content="Calculate" Width="200" Height="38" Background="#3498DB" Foreground="White" FontSize="15" FontWeight="Bold" Click="CalculateArea" BorderThickness="0" Margin="0,0,0,20" Cursor="Hand">
                            <Button.Style>
                                <Style TargetType="Button">
                                    <Setter Property="Background" Value="#3498DB"/>
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="Button">
                                                <Border Background="{TemplateBinding Background}" CornerRadius="20" BorderThickness="0">
                                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                </Border>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </Button.Style>
                        </Button>
                        <Border Background="#ECF0F1" Padding="15" CornerRadius="5" BorderBrush="#BDC3C7" BorderThickness="1">
                            <TextBlock x:Name="txtAreaResult" Text="Enter Value and click Calculate" FontSize="13" Foreground="#2C3E50"/>
                        </Border>
                    </StackPanel>
                </Grid>
            </TabItem>
            <TabItem Header="Pythagoras" FontSize="13">
                <Grid Margin="20">
                    <StackPanel>
                        <TextBlock Text="Solve a² + b² = c²" FontSize="13" FontWeight="SemiBold" Foreground="#34495E" Margin="0,0,0,20"/>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
                            <TextBlock Text="a = " Width="40" VerticalAlignment="Center" FontSize="14" FontWeight="SemiBold"/>
                            <TextBox x:Name="txta" Width="200" Height="32" FontSize="14" Padding="8,5"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
                            <TextBlock Text="b = " Width="40" VerticalAlignment="Center" FontSize="14" FontWeight="SemiBold"/>
                            <TextBox x:Name="txtb" Width="200" Height="32" FontSize="14" Padding="8,5"/>
                        </StackPanel>
                        <Button x:Name="btnCalculatePythagoras" Content="Calculate" Width="200" Height="38" Background="#3498DB" Foreground="White" FontSize="15" FontWeight="Bold" Click="CalculatePythagoras" BorderThickness="0" Margin="0,0,0,20" Cursor="Hand">
                            <Button.Style>
                                <Style TargetType="Button">
                                    <Setter Property="Background" Value="#3498DB"/>
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="Button">
                                                <Border Background="{TemplateBinding Background}" CornerRadius="20" BorderThickness="0">
                                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                </Border>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </Button.Style>
                        </Button>
                        <Border Background="#ECF0F1" Padding="15" CornerRadius="5" BorderBrush="#BDC3C7" BorderThickness="1">
                            <TextBlock x:Name="txtPythagorasResult" Text="Enter Values and click Calculate" FontSize="13" Foreground="#2C3E50"/>
                        </Border>
                    </StackPanel>
                </Grid>
            </TabItem>
        </TabControl>
    </Grid>
</Window>
